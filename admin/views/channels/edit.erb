<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:channels, :index) %></li>
      <li><%= link_to pat(:new), url(:channels, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:channels, :edit, :id => @channel.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:channel) %></h2>
    <div class="inner">
      <% form_for :channel, url(:channels, :update, :id => @channel.id), :method => :put, :class => :form do |f| %>
        <%= partial "channels/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
