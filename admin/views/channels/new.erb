<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:channels, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:channels, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:channel) %></h2>
    <div class="inner">
      <% form_for :channel, url(:channels, :create), :class => :form do |f| %>
        <%= partial "channels/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
